name = "lawyer-ad-checker"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# 환경변수는 Cloudflare Dashboard > Pages > Settings > Environment variables 에서 설정
# ANTHROPIC_API_KEY = "sk-ant-..."
